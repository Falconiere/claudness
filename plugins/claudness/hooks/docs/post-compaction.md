Context compacted. Recover memories before continuing (the engram wrapper
ships in the code-intel plugin: `<code-intel plugin root>/skills/code-intel/scripts/mod.sh`):
1) `mod.sh engram summary "## Goal\n...\n## Accomplished\n...\n## Next Steps\n..."`
2) `mod.sh engram context`
