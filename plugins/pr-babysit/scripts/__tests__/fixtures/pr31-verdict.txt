**Claude finished @Falconiere's task in 4m 23s** —— [View job](https://github.com/Falconiere/claudness/actions/runs/27481505059)

---
### Code Review — `feat(statusliner): extract statusline into standalone optional plugin`

- [x] Read repository context and PR diff
- [x] Review changed files
- [x] Analyze correctness, security, performance
- [x] Post findings
- [x] Set verdict label (`agent-merge-approved`)

### Findings

`plugins/claudness/hooks/session-start.sh:17`: low: Comment calls the sweep "Transitional one-time" but the block runs on every SessionStart forever (it just no-ops once the symlink is gone). Reword to "Transitional orphan sweep" or add a "remove after vX.Y" marker so a future maintainer doesn't assume it can be dropped without a deprecation window.
`plugins/claudness/hooks/session-start.sh:23`: low: The sweep silently removes a working symlink for users upgrading mid-session. Their `settings.json` (still pointing at the old path) will start erroring on next render with no actionable hint. Consider appending a one-shot WARN into `parts` whenever the sweep fires (e.g. `parts+=("WARN: statusline moved to the 'statusliner' plugin — install /plugin install statusliner@falconiere and re-point settings.json to bash ~/.claude/statusliner/statusline.sh")`) so the user sees what happened in-session. [Fix this →](https://claude.ai/code?q=In%20plugins%2Fclaudness%2Fhooks%2Fsession-start.sh%20on%20the%20feat%2Fstatusliner-plugin%20branch%2C%20when%20the%20orphan%20sweep%20at%20line%2024%20removes%20the%20legacy%20statusline%20symlink%2C%20append%20a%20one-shot%20WARN%20to%20%24parts%20telling%20the%20user%20to%20install%20statusliner%40falconiere%20and%20re-point%20settings.json%20to%20bash%20~%2F.claude%2Fstatusliner%2Fstatusline.sh.%20Set%20a%20flag%20before%20the%20config-load%20block%20and%20push%20the%20WARN%20later%20where%20other%20parts%20are%20appended.%20Add%20a%20bats%20test%20in%20plugins%2Fclaudness%2Fhooks%2F__tests__%2Fsession-start.bats%20asserting%20the%20WARN%20appears%20when%20a%20legacy%20symlink%20existed.&repo=Falconiere/claudness)
`plugins/claudness/hooks/__tests__/session-start.bats`: low: PR description claims "orphan-sweep smoke (dangling symlink removed, real file preserved)" was verified, but no bats test codifies the new sweep behavior at `session-start.sh:23-24`. Add two cases: (i) a pre-existing `-L` symlink at `$CFG/claudness/statusline.sh` is removed; (ii) a real (non-symlink) file at that same path survives. The setup mirrors the new `plugins/statusliner/hooks/__tests__/session-start.bats` pattern. [Fix this →](https://claude.ai/code?q=Add%20two%20bats%20test%20cases%20to%20plugins%2Fclaudness%2Fhooks%2F__tests__%2Fsession-start.bats%20on%20the%20feat%2Fstatusliner-plugin%20branch%20to%20cover%20the%20orphan%20sweep%20introduced%20in%20session-start.sh%3A23-24.%20Case%201%3A%20pre-create%20a%20symlink%20at%20%24TMP%2Fcfg%2Fclaudness%2Fstatusline.sh%20pointing%20at%20a%20fake%20target%2C%20run%20the%20hook%20with%20CLAUDE_CONFIG_DIR%3D%24TMP%2Fcfg%2C%20assert%20the%20symlink%20is%20gone.%20Case%202%3A%20pre-create%20a%20REAL%20file%20at%20the%20same%20path%2C%20run%20the%20hook%2C%20assert%20the%20file%20survives%20unchanged.%20Follow%20the%20existing%20setup%2Fteardown%20conventions%20in%20the%20file.&repo=Falconiere/claudness)
`plugins/statusliner/hooks/__tests__/session-start.bats:22`: low: `[[ "$target" == *"/statusline.sh" ]]` only asserts the target ends in `statusline.sh`, so a misrouted symlink (pointing to, say, claudness's leftover script if both plugins were installed) would still pass. Tighten to also assert the target lives under the statusliner plugin dir, e.g. `[[ "$target" == *"/statusliner/statusline.sh" ]]`.
`plugins/statusliner/hooks/session-start.sh:24`: low: `mkdir -p ... || exit 0` swallows the failure cause; if a user has a real file (not a dir) at `$CLAUDE_CONFIG_DIR/statusliner`, the hook silently no-ops every session. Acceptable for a non-fatal hook but a stderr breadcrumb (`>&2 echo "statusliner: cannot create $reg_root"`) would shorten the eventual debug. Skip if you prefer the current strict-silence model.
`plugins/statusliner/statusline.sh:71`: low: Pre-existing, not new in this PR — gate-root resolution runs `git -C "$cwd" rev-parse --show-toplevel` plus `symbolic-ref --short HEAD` on every render. Hot path: two git invocations per prompt. Not a regression, just flagging it for the file's new owner.

### Other checks
- `jq .` passes on `marketplace.json`, `plugin.json`, `hooks.json`.
- Shellcheck-clean by inspection on `statusliner/hooks/session-start.sh`, `statusliner/statusline.sh`, and the trimmed `claudness/hooks/session-start.sh`.
- Symlink guard (`-L` or `! -e`) correctly prevents clobbering a user-owned real file — matches the test in `plugins/statusliner/hooks/__tests__/session-start.bats:35-42`.
- Rename history preserved (verified via `git log --oneline --all -- plugins/claudness/__tests__/statusline.bats`).
- No new security exposure: caveman flag still capped (`head -c 64`), charset-stripped, mode whitelisted; gate reader still defends against absent / malformed JSON.
- Marketplace entry has no `dependencies` as documented; gate + caveman segments degrade gracefully when source files are absent.
- The `registry.sh` source removal from `claudness/hooks/session-start.sh` is safe — verified the rest of that file uses only `config.sh` (`claudness_enabled`) and `detect.sh` (`detect_*`), no `claudness_registry_*` callers remain.

### Top-3 must-fix
1. **`claudness/hooks/session-start.sh:23`** — silent breakage on upgrade. Append a one-shot WARN to `parts` when the sweep fires so users see the actionable migration hint in-session instead of debugging a "missing file" error from `statusLine`.
2. **`claudness/hooks/__tests__/session-start.bats`** — add bats coverage for the orphan sweep (symlink removed + real file preserved). PR notes claim smoke-tested but the suite doesn't lock the invariant in.
3. **`claudness/hooks/session-start.sh:17`** — fix the "one-time" wording, or schedule a removal version, so future you doesn't drop the sweep prematurely.

All three are improvements, not blockers — the PR is correct, well-scoped, and safe. **Approved** (`agent-merge-approved`).
 · [Branch](https://github.com/Falconiere/claudness/tree/feat/statusliner-plugin)
