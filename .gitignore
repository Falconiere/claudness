.claude/tmp/
# Atomic-write leftovers (e.g. interrupted editor writes: file.sh.tmp.PID.HASH)
*.tmp.*

# Local workflow artifacts (specs/plans) — not shipped
docs/claudness/

# Dependencies
node_modules/
