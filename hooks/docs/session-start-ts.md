## TypeScript notes
- TS tests must live in co-located `__tests__/` dirs (sibling of source, flat — only fixtures/helpers/mocks/utils subdirs).
- Run your project's typecheck (`{{node_pm}} run typecheck`) before claiming a change works.
