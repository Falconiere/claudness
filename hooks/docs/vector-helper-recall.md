Before exploring code, recall from memory first:

`.claude/skills/code-intel/scripts/mod.sh engram search "<topic>"` — check what was already figured out.
`.claude/skills/code-intel/scripts/mod.sh engram context` — on session start, after compaction, or after clear.

Never skip for optimization, refactor, or any task that touches existing code.
If search returns relevant results, read them before touching any file.
