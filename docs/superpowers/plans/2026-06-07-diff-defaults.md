| # | Path | R lines | Y lines | What differs | Default | Why |
|---|------|---------|---------|--------------|---------|-----|
| 1 | .claude/agents/deep-explore.md | 60 | 46 | R uses grepai semantic search + call-graph; Y uses ast-grep structural | Y | Y aligns with the ast-grep + engram-only direction we are migrating to |
| 2 | .claude/commands/commit.md | 11 | 13 | R calls `./scripts/ts-check.sh --staged`; Y calls `./tools/yamless/check.sh ts-staged` + rust-staged + "never git push" | Y | Y adds rust gate + push block; project-specific paths get rewritten anyway |
| 3 | .claude/commands/review-and-commit.md | 37 | 39 | R uses `main` + `bun run check`; Y uses `development` + yamless wrappers + rust + never-push | Y | Y is additive (rust gate, push block); base branch and paths will be re-templated |
| 4 | .claude/hooks/docs/session-start.md | 26 | 34 | R: routo branding + engram recall mandate; Y: yamless branding + rust rules, jscpd 3%, tmux observability, never-push | Y | Y has substantially more current policy (rust, jscpd, observability, push block) |
| 5 | .claude/hooks/docs/vector-helper-recall.md | 16 | 7 | R has 6-layer recall (engram→grepai→gitnexus→ast-grep→Grep); Y has 2-layer (engram via mod.sh) | Y | Drops gitnexus/grepai references we are not migrating; Y matches mod.sh wrapper |
| 6 | .claude/hooks/docs/vector-helper-save.md | 10 | 5 | R uses bare `engram save` with Call Graph section; Y uses `mod.sh engram save`, no call graph | Y | Y uses the wrapper we are standardizing on; drops grepai call-graph dep |
| 7 | .claude/hooks/post-tools/modules/gate-status.sh | 76 | 67 | R has ts-quality-hook lifecycle guard + `source` field; Y regex covers yamless wrappers, rust, nextest | M | R has a real lifecycle safety check Y lacks; Y has rust/wrapper regex R lacks — both useful |
| 8 | .claude/hooks/post-tools/modules/ts-quality.sh | 254 | 113 | R adds Edit-tool support, worktree skip, `as`-cast check, gate-status writes; Y is older slimmer version | R | R is the actively-maintained, more capable version; Y dropped real checks |
| 9 | .claude/hooks/pre-compact.sh | 18 | 18 | Fallback string only: R says `engram`, Y says `mod.sh engram` | Y | Y matches the mod.sh wrapper standard |
| 10 | .claude/hooks/pre-tools/modules/bash-commands.sh | 111 | 88 | R enforces `--project` on engram + has push block commented out; Y blocks `git push main/master` + `cargo test` (use nextest) + updates wrapper paths | Y | Y has active push + cargo-test blocks; engram --project handled by mod.sh now |
| 11 | .claude/hooks/pre-tools/modules/code-edit-rules.sh | 35 | 38 | Y adds `.rs` → rust-quality rules branch, adds `.py` to TS bucket | Y | Y adds rust + python coverage; purely additive |
| 12 | .claude/hooks/pre-tools/modules/commit-gate.sh | 24 | 24 | R reminds `bun run check` + bare engram save; Y reminds diff-stat + mod.sh engram save | Y | Y uses mod.sh wrapper; reminder text easily re-templated |
| 13 | .claude/hooks/pre-tools/modules/protected-files.sh | 28 | 40 | Y adds protection for `tools/yamless/cmd/check.sh` and `lib/checker.sh` | Y | Y is purely additive (extra protected paths) |
| 14 | .claude/hooks/pre-tools/modules/quality-gate.sh | 70 | 35 | R DENIES on failing gate (with worktree skip + allowlists); Y only WARNS via additionalContext | M | Different policy intent: R is stricter enforcement, Y is softer — owner must choose stance |
| 15 | .claude/hooks/pre-tools/modules/search-nudge.sh | 95 | 91 | R nudges Grep→grepai semantic, sed→ast-grep; Y nudges Grep→ast-grep structural, has STRUCT_RE pattern | Y | Y drops grepai (not migrating) and adds rust STRUCT_RE for ast-grep routing |
| 16 | .claude/hooks/session-end.sh | 14 | 14 | Fallback string: R `engram save`, Y `mod.sh engram save` | Y | Y matches mod.sh wrapper standard |
| 17 | .claude/hooks/session-start.sh | 108 | 100 | R starts grepai watcher, mentions engram bare; Y drops grepai watcher, adds rust/jscpd/push reminders | Y | Y drops grepai (not migrating) and has fuller policy reminder |
| 18 | .claude/hooks/user-prompt-submit.sh | 122 | 125 | R has grepai+gitnexus mandate hints, no push hint; Y has ast-grep mandate, rust test dir, push-block, jscpd, libsql migration hint | Y | Y replaces grepai/gitnexus prompts with ast-grep and adds push/rust/jscpd hints |
| 19 | .claude/settings.json | 122 | 118 | R allows `mod.sh` and has remote/agentConfig/mcpServers blocks; Y adds `git push`, `ast-grep`, `sg` to allow, adds push-to-main denies, adds mcp-blocker hook, swaps mcpjsonServers (claude-flow→auggie), drops github plugin | M | stash both raw, build fragments in Phase 5 |
| 20 | .claude/skills/code-intel/scripts/mod.sh | 37 | 35 | R routes to grepai/engram/gitnexus; Y routes to engram/ast-grep | Y | R references gitnexus + grepai modules we are NOT migrating |
| 21 | .claude/skills/code-intel/scripts/modules/engram.sh | 119 | 63 | R hardcodes `PROJECT=routo.io` with awk filter for strict project filtering; Y hardcodes `yamless`, no filter | M | R's strict cross-project filter logic is genuinely better; Y is the slimmer baseline — owner picks |
| 22 | .claude/skills/code-intel/SKILL.md | 102 | 63 | R documents grepai semantic + gitnexus impact/context + cypher ref; Y documents ast-grep + engram only | Y | R references gitnexus + grepai modules we are NOT migrating |
| 23 | .claude/skills/context7/SKILL.md | 48 | 50 | Y adds `--fast` flag docs, version-pin syntax, points `.env` to `.tooling/.env` | Y | Purely additive doc improvements + shared env path |
| 24 | .claude/skills/exa-search/SKILL.md | 76 | 78 | Y updates type/category lists, adds `--lean` flag, rewrites constraints table | Y | Y matches newer Exa API surface + adds lean output |
| 25 | .tooling/context7/search.sh | 137 | 145 | Y reads from `../​.env` (shared tooling env), adds `--fast` flag plumbing | Y | Shared env + additive `--fast` support |
| 26 | .tooling/exa-search/search.sh | 197 | 215 | Y reads from `../​.env` (shared tooling env), adds `--lean` jq projection | Y | Shared env + additive `--lean` support |
